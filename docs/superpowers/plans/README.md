# Plans

Implementation plans live here after a spec or clear requirement is ready to execute.

Use the filename format:

```text
YYYY-MM-DD-topic.md
```

Each plan should break work into small, verifiable steps and include commit boundaries.
