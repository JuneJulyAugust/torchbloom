# Specs

Design specs live here when a task needs durable agreement before implementation.

Use the filename format:

```text
YYYY-MM-DD-topic-design.md
```

Each spec should name the goal, scope, non-goals, architecture or artifact model, verification approach, and open questions.
