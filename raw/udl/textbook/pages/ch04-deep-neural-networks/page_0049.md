---
source: UnderstandingDeepLearning_02_09_26_C.pdf
page_key: 49
book_page: 49
pdf_page: 63
chapter: "4 - Deep neural networks"
chapter_slug: ch04-deep-neural-networks
ocr_sources:
  - deepseek-ocr-2
  - ppstructurev3
fusion_status: fused
confidence: medium
figure_count: 0
---
or even more compactly in matrix notation as:

$$
\begin{aligned}
\begin{array}{rcl}\mathbf{h}&=&\mathbf{a}[\boldsymbol{\theta}_{0}+\boldsymbol{\theta}x]\\\mathbf{h}^{\prime}&=&\mathbf{a}[\psi_{0}+\boldsymbol{\Psi}\mathbf{h}]\\y^{\prime}&=&\phi_{0}^{\prime}+\boldsymbol{\phi}^{\prime}\mathbf{h}^{\prime},\end{array} \tag{4.14}
\end{aligned}
$$

where, in each case, the function a[•] applies the activation function separately to every element of its vector input.

## 4.4.1 General formulation

This notation becomes cumbersome for networks with many layers. Hence, from now on, we will describe the vector of hidden units at layer k as  $h_{k}$ , the vector of biases (intercepts) that contribute to hidden layer  $k+1$  as  $\beta_{k}$ , and the weights (slopes) that are applied to the  $k^{th}$  layer and contribute to the  $(k+1)^{th}$  layer as  $\Omega_{k}$ . A general deep network  $y = f[x, \phi]$  with K layers can now be written as:

$$
\begin{aligned}
\begin{array}{rcl}\mathbf{h}_{1}&=&\mathbf{a}[\boldsymbol{\beta}_{0}+\Omega_{0}\mathbf{x}]\\\mathbf{h}_{2}&=&\mathbf{a}[\boldsymbol{\beta}_{1}+\Omega_{1}\mathbf{h}_{1}]\\\mathbf{y}&=&\boldsymbol{\beta}_{K}+\Omega_{K}\mathbf{h}_{K}.\end{array} \tag{4.15}
\end{aligned}
$$

The parameters $\phi$ of this model comprise all of these weight matrices and bias vectors $\boldsymbol{\phi} = \left\lbrace \beta_{k}, \Omega_{k}\right\rbrace_{k=0}^{K}$.

If the $k^{th}$ layer has $D_{k}$ hidden units, then the bias vector $\beta_{k-1}$ will be of size $D_{k}$. The last bias vector $\boldsymbol{\beta}_{K}$ has the size $D_{o}$ of the output. The first weight matrix $\Omega_{0}$ has size $D_{1} \times D_{i}$ where $D_{i}$ is the size of the input. The last weight matrix $\Omega_{K}$ is $D_{o} \times D_{K}$, and the remaining matrices $\Omega_{k}$ are $D_{k+1} \times D_{k}$ (figure 4.6).

We can equivalently write the network as a single function:

$$
\begin{aligned}
\mathbf{y}=\boldsymbol{\beta}_{K}+\Omega_{K}\mathbf{a}\left[\boldsymbol{\beta}_{K-1}+\Omega_{K-1}\mathbf{a}[\cdots\boldsymbol{\beta}_{2}+\Omega_{2}\mathbf{a}[\boldsymbol{\beta}_{1}+\Omega_{1}\mathbf{a}[\boldsymbol{\beta}_{0}+\Omega_{0}\mathbf{x}]]\cdots\right]. \tag{4.16}
\end{aligned}
$$

## 4.5 Shallow vs. deep neural networks

Chapter 3 discussed shallow networks (with a single hidden layer), and here we have described deep networks (with multiple hidden layers). We now compare these models.
