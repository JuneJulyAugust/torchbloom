---
source: UnderstandingDeepLearning_02_09_26_C.pdf
page_key: 278
book_page: 278
pdf_page: 292
chapter: "15 - Generative adversarial networks"
chapter_slug: ch15-generative-adversarial-networks
ocr_sources:
  - deepseek-ocr-2
  - ppstructurev3
fusion_status: fused
confidence: medium
figure_count: 0
---
## 15.1.1 GAN loss function

We now define the loss function for training GANs more precisely. The discriminator  $f[x, \phi]$  takes input x, has parameters  $\phi$ , and returns a scalar that is higher when it believes the input is a real example. This is a binary classification task, so we adapt the binary cross-entropy loss function (section 5.4), which originally had the form:

$$
\begin{aligned}
\hat{\phi}=\underset{\boldsymbol{\phi}}{\mathrm{a r g m i n}}\left[\sum_{i}-(1-y_{i})\log\left[1-\mathrm{sig}[f[\mathbf{x}_{i},\boldsymbol{\phi}]]\right]-y_{i}\log\left[\mathrm{sig}[f[\mathbf{x}_{i},\boldsymbol{\phi}]]\right]\right],
\tag{15.2}
\end{aligned}
$$

where $y_{i} \in \lbrace 0,1 \rbrace$ is the label, and $\mathrm{sig}[\bullet]$ is the logistic sigmoid function (figure 5.7).

In this case, we assume that the real examples x have label y = 1 and the generated samples  $x^{*}$  have label y = 0 so that:

$$
\begin{aligned}
\hat{\phi}=\underset{\boldsymbol{\phi}}{\mathrm{a r g m i n}}\left[\sum_{j}-\log\left[1-\mathrm{sig}[f[\mathbf{x}_{j}^{*},\boldsymbol{\phi}]]\right]-\sum_{i}\log\left[\mathrm{sig}[f[\mathbf{x}_{i},\boldsymbol{\phi}]]\right]\right],
\tag{15.3}
\end{aligned}
$$

where i and j index the real examples and generated samples, respectively.

Now we substitute the definition for the generator  $x_{j}^{*} = g[z_{i}, \theta]$  and note that we must maximize with respect to  $\theta$  since we want the generated samples to be misclassified (i.e., have low likelihood of being synthetic or high negative log-likelihood):

$$
\begin{aligned}
\hat{\theta}=\underset{\boldsymbol{\theta}}{\mathrm{a r g m a x}}\left[\min_{\phi}\sum_{j}-\log\left[1-\mathrm{sig}[f[\mathbf{g}[\boldsymbol{z}_{j},\boldsymbol{\theta}]],\phi]\right]-\sum_{i}\log\left[\mathrm{sig}[f[\mathbf{x}_{i},\boldsymbol{\phi}]]\right]\right].
\tag{15.4}
\end{aligned}
$$

## 15.1.2 Training GANs

Equation 15.4 is a more complex loss function than we have seen before; the discriminator parameters $\phi$ are manipulated to minimize the loss function, and the generative parameters $\theta$ are manipulated to maximize the loss function. GAN training is characterized as a minimax game; the generator tries to find new ways to fool the discriminator, which in turn searches for new ways to distinguish generated samples from real examples. Technically, the solution is a Nash equilibrium — the optimization algorithm searches for a position that is simultaneously a minimum of one function and a maximum of the other. If training proceeds as planned, then upon convergence, $\hat{\mathbf{g}}[\mathbf{z}, \boldsymbol{\theta}]$ will be drawn from the same distribution as the data, and $\mathrm{sig}[\mathbf{f}[\bullet, \boldsymbol{\phi}]]$ will be at chance (i.e., 0.5).

To train the GAN, we can divide equation 15.4 into two loss functions:

$$
\begin{aligned}&L[\phi]\quad=\quad\sum_{j}-\log\left[1-\mathrm{sig}[f[\mathbf{g}[\boldsymbol{z}_{j},\theta],\phi]]\right]-\sum_{i}\log\left[\mathrm{sig}[f[\mathbf{x}_{i},\phi]]\right]\\&L[\boldsymbol{\theta}]\quad=\quad\sum_{j}\log\left[1-\mathrm{sig}[f[\mathbf{g}[\boldsymbol{z}_{j},\theta],\phi]]\right],\\ \end{aligned}
\tag{15.5}
$$
