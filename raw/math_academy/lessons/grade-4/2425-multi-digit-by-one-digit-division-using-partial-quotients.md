# Multi-Digit by One-Digit Division Using Partial Quotients

Source: https://www.mathacademy.com/topics/2425?courseId=75
Topic ID: 2425

## Prerequisites

- [More Multi-Digit by One-Digit Division Using Area Models](./3920-more-multi-digit-by-one-digit-division-using-area-models.md)
- [Two-Digit by One-Digit Division With Remainders Using Partial Quotients](./3925-two-digit-by-one-digit-division-with-remainders-using-partial-quotients.md)

## Lesson

### Introduction

Box models are powerful because they provide a general method for dividing large numbers. And, as we've seen, they can also handle cases with remainders.

Let's consider the following three-digit by one-digit division problem.

$$

630 \div 3

$$

We set up our box model in the usual way.

We now follow our usual four steps:

**Step 1**: Pick a (fairly large) multiple of $3$ that is easy to compute but is no larger than $630.$ Let's pick $3 \times {\color{blue}200} = 600.$

We write ${\color{blue}200}$ on top of the box and subtract $3 \times{\color{blue}200}$ from $630$ inside the box.

**Step 2**: Next, we bring ${\color{red}30}$ to the right.

**Step 3**: Pick a multiple of $3$ that is easy to compute but is no larger than $30.$ Let's pick $3 \times {\color{blue}10} = 30.$

We write $\color{blue}10$ above the box and subtract $3 \times {\color{blue}10}$ from $\color{red}30$ inside the box.

We get $\fbox{0},$ so the division is done.

**Step 4**: To find the quotient of $630 \div 3,$ we add the numbers on top of the boxes:

$$

200+10=210

$$

Therefore,

$$

630 \div 3 = 210.

$$

Let's now look at an example with a remainder.

### Example: Dividing Three-Digit Numbers Using Box Models With Scaffolding

#### Question

Use the box model above to compute the quotient of $421 \div 4.$

#### Explanation

First, we subtract $4 \times {\color{blue}100} = 400$ from $421.$

Next, we bring $\color{red}21$ to the right.

Then, we pick a multiple of $4$ that is easy to compute but is no larger than ${\color{red}21}.$ Let's pick $4 \times {\color{blue}5} = 20.$

We write $\color{blue}5$ above the box and subtract $4 \times {\color{blue}5}$ from $\color{red}21$ inside the box.

We can't subtract any further since $\fbox{1}$ is less than $4.$ Hence, $1$ is the remainder.

The quotient is the sum of the numbers on top of the boxes:

$$

100+5=105

$$

Therefore,

$$

421 \div 4 = 105 \,\text{R}\, 1.

$$

### Example: Dividing Three-Digit Numbers Using Box Models

#### Question

Laura paid $144$ dollars for $6$ concert tickets. Using the box model above, find out how much Laura paid for each ticket.

#### Explanation

To find out how much Laura paid for each ticket, we need to divide $144$ by $6.$

First, we subtract $6 \times {\color{blue}{20}}=120$ from $144.$

Next, we bring $\color{red}24$ to the right.

Then, we pick a multiple of $6$ that is easy to compute but is no larger than ${\color{red}24}.$ Let's pick $6 \times {\color{blue}4} = 24.$

We write $\color{blue}4$ above the box and subtract $6 \times {\color{blue}4}$ from $\color{red}24$ inside the box.

We get $\fbox{0},$ so the division is done.

The quotient is the sum of the numbers on top of the boxes:

$$

20+4=24

$$

So,

$$

144\div 6 =24

$$

Therefore, Laura paid $24$ for each ticket.

### Box Models With Three Boxes

Sometimes, we might want to use three (or more) boxes to solve a division problem. This often happens when the dividend in our division problem is large.

Although there is no limit to how many boxes we can use, we'll stick to a maximum of three boxes in this lesson.

Let's see an example.

### Example: Dividing Three-Digit Numbers Using Box Models With Three Boxes

#### Question

Use the box model above to find the value of $778 \div 6.$

#### Explanation

First, we subtract $6 \times {\color{blue}100} = 600$ from $778.$

Next, we bring $\color{red}178$ to the right and subtract $6 \times {\color{blue}20} = 120$ from it.

Next, we bring ${\color{red}58}$ to the right.

Finally, we pick a multiple of $6$ that is easy to compute but is no larger than ${\color{red}58}.$ Let's pick $6 \times {\color{blue}9} = 54.$

We write $\color{blue}9$ above the box and subtract $6 \times {\color{blue}9}$ from $\color{red}58$ inside the box.

We can't subtract any further since $\fbox{4}$ is less than $6.$ Hence, $4$ is the remainder.

The quotient is the sum of the numbers on top of the boxes:

$$

100+20+9=129

$$

Therefore,

$$

778\div 6 = 129 \,\text{R}\, 4.

$$
